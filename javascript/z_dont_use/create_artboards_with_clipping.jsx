#target Illustratorfunction createABwithClipping(){    if (app.documents.length > 0){        var docRef = app.activeDocument;        var aB = docRef.artboards;        var docLayers = docRef.layers;        var layer1 = docLayers[0];        var groups = layer1.groupItems;        //~ var artGrouped = confirm("Is your artwork properly grouped?" + ('\n') + "If you aren't sure, please ask");        function removeAB(){ // removes all current artboards // this precludes the possibility that double artboards may be present                        while (aB.length > 1){                for (r=1; r < aB.length; r++){                    aB[r].remove();                                        } // end for loop R                } // end while loop            }// end function removeAB();        removeAB();/*    if (groups.pathItems.length > 0){        alert("Your art is not grouped properly");        return;        }*/    function addAB(){ //add artboards around top level groups//~         alert(groups.length);        for (a = 0; a < groups.length; a++){            docRef.selection = null;            groups[a].selected = true;            docRef.fitArtboardToSelectedArt(a);            var vB = groups[a].visibleBounds;            aB.add(vB);                            }// end for loop        } // end function addAB();        addAB();/*    function removeBLankAB(){        for (b = 0; b < aB.length; b++){            var paths = docRef.selectObjectsOnActiveArtboard();            var sel = docRef.selection;            if (sel.length > 0){                } // end if objects selected            else {                if (aB.length > 0){                    aB[b].remove();                    }                }            }            */            if (aB.length > groups.length){        aB[aB.length-1].remove();        } // end if            } // end if exist document    else{        alert("No Document Open");v        } // end else no doc open} // end function createABcreateABwithClipping();