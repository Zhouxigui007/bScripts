#target illustrator-18function adjustmentLog(){	var docRef = app.activeDocument;	var layers = docRef.layers;	var adjustmentLayer = null;	try {		adjustmentLayer = layers.getByName("Adjustment Log");		adjustmentLayer.zOrder (ZOrderMethod.SENDTOBACK);	}	catch(err){		adjustmentLayer = layers.add();		adjustmentLayer.name = "Adjustment Log";		adjustmentLayer.zOrder (ZOrderMethod.SENDTOBACK);	}	docRef.selection = null;	docRef.selectObjectsOnActiveArtboard();	var sel = docRef.selection;	for (i=0; i<sel.length; i++){		sel[i].move(adjustmentLayer, ElementPlacement.PLACEATEND);	}	adjustmentLayer.locked = true;}adjustmentLog();